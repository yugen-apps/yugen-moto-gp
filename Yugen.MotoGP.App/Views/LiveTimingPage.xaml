<Page x:Class="Yugen.MotoGP.App.Views.LiveTimingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Yugen.MotoGP.App.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="using:Yugen.MotoGP.App.Models"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="4"
                       Text="{x:Bind ViewModel.Head.event_tv_name, Mode=OneWay}" />

            <TextBlock Margin="4"
                       Text="{x:Bind ViewModel.Head.remaining, Mode=OneWay}" />

            <TextBlock Margin="4"
                       Text="{x:Bind ViewModel.Head.category, Mode=OneWay}" />
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.RiderCollection, Mode=OneWay}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <TextBlock Padding="2"
                                   Text="P" />

                        <TextBlock Grid.Column="1"
                                   Padding="2"
                                   Text="Name" />

                        <TextBlock Grid.Column="2"
                                   Padding="2"
                                   Text="" />

                        <TextBlock Grid.Column="3"
                                   Padding="2"
                                   Text="#" />

                        <TextBlock Grid.Column="4"
                                   Padding="2"
                                   Text="Last Lap Time" />

                        <TextBlock Grid.Column="5"
                                   Padding="2"
                                   Text="Best Lap Time" />

                        <TextBlock Grid.Column="6"
                                   Padding="2"
                                   Text="Lap" />

                        <TextBlock Grid.Column="7"
                                   Padding="2"
                                   Text="Gap P." />

                        <TextBlock Grid.Column="8"
                                   Padding="2"
                                   Text="Gap F." />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Rider">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Border Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                            <TextBlock Padding="2"
                                       Text="{x:Bind pos}" />
                        </Border>

                        <TextBlock Grid.Column="1">
                            <Run Text="{x:Bind rider_name}" />
                            <Run Text=" " />
                            <Run Text="{x:Bind rider_surname}" />
                        </TextBlock>

                        <TextBlock Grid.Column="2"
                                   Padding="2"
                                   Text="{x:Bind on_pit}" />

                        <Border Grid.Column="3"
                                Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                            <TextBlock Padding="2"
                                       Text="{x:Bind rider_number}" />
                        </Border>

                        <Border Grid.Column="4"
                                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                            <TextBlock Padding="2"
                                       Text="{x:Bind last_lap_time}" />
                        </Border>


                        <Border Grid.Column="5"
                                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                            <TextBlock Padding="2"
                                       Text="{x:Bind lap_time}" />
                        </Border>


                        <TextBlock Grid.Column="6">
                            <Run Text="{x:Bind num_lap}" />
                            <Run Text="/" />
                            <Run Text="{x:Bind last_lap}" />
                        </TextBlock>

                        <TextBlock Grid.Column="7"
                                   Padding="2"
                                   Text="{x:Bind gap_prev}" />

                        <TextBlock Grid.Column="8"
                                   Padding="2"
                                   Text="{x:Bind gap_first}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
